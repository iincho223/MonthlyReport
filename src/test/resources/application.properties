# 本番と同じ検証を効かせ、エンティティと Flyway DDL の型不整合をテストで検出する。
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# 接続情報と方言は AbstractIntegrationTest の @ServiceConnection が
# Testcontainers(MariaDB) から解決するため、ここでは指定しない。
